// Como acceder a los valores de un objeto

const producto = {
  nombre: 'Monitor 20"',
  precio: 300,
  disponible: true,
}

console.log(producto) // Todo el conjunto
console.log(producto.nombre) // Llamo un propiedad en particular con la declaración por punto
console.log(producto['precio']) // Llamo un propiedad en particular con los corchetes
